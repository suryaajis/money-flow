services:
  api:
    image: suryaajis/money-flow-api-dev:latest
    container_name: money-flow-api-dev
    restart: unless-stopped
    env_file: .env
    networks:
      - nginx-proxy

networks:
  nginx-proxy:
    external: true
