"use client";

// Data is now loaded from the server in AppShell via fetchTransactions/fetchCategories.
// This component is kept as a placeholder for future client-side bootstrapping needs.
export const DataBootstrap: React.FC = () => null;
